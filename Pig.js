class Pig extends BaseClass {
    constructor(x, y) {
     super(x,y,50,50);//super is used for transferring the all the properties of the parent class to child class
     this.image=loadImage("sprites/enemy.png");
       
      }
     
    }